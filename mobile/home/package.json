{
  "name": "@mobile/home",
  "version": "0.0.1",
  "description": "Mobile - Home",
  "main": "dist/umd/index.js",
  "module": "dist/es5/index.js",
  "typings": "dist/types/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "clean-build": "rimraf 'build'",
    "clean-dist": "rimraf 'dist'",
    "prebuild": "yarn clean-build && yarn clean-dist",
    "build": "tsc -p tsconfig.json",
    "doc": "typedoc --out dist/docs --target es6 --theme minimal src",
    "predist": "yarn build",
    "dist":
      "babel build -d dist/es5 && babel build -d dist/umd --plugins transform-es2015-modules-umd",
    "postdist": "yarn clean-build && yarn doc",
    "prepublish": "yarn dist"
  },
  "peerDependencies": {
    "react": "*",
    "react-native": "*"
  },
  "dependencies": {
    "@mobile/themes": "0.0.1",
    "@mobile/config": "0.0.1",
    "styled-components": "2.4.0"
  },
  "devDependencies": {
    "babel-core": "6.26.0",
    "babel-cli": "6.26.0",
    "typescript": "2.6.2",
    "typedoc": "0.9.0",
    "rimraf": "2.6.2"
  },
  "license": "MIT"
}
